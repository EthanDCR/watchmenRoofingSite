/** @type {import('next').NextConfig} */
const nextConfig = {
  // CSS Modules are on by default in Next.js — nothing to enable.
  // No Tailwind, no external CSS frameworks.
};

module.exports = nextConfig;
